<div class="global">
    <nav class="navbar navbar-default navbar-fixed-top col-lg-12">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">サイト名</a>
        </div>

        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <% if user_signed_in? %>
              <li><a href="/mypage">マイページ</a></li>
            <% end %>
            <li<% if params[:controller] == 'grade1' %><%= ' class=active' %><% end %>><%= link_to '中学1年生', grade1_index_path %></li>
            <li<% if params[:controller] == 'grade2' %><%= ' class=active' %><% end %>><%= link_to '中学2年生', grade2_index_path %></li>
            <li<% if params[:controller] == 'grade3' %><%= ' class=active' %><% end %>><%= link_to '中学3年生', grade3_index_path %></li>
          </ul>
          <div class="form-group navbar-form navbar-left">
            <%= search_form_for @search, url: lessons_path do |f| %>
              <%= f.text_field :summary_or_title_or_grade_or_category_name_or_category_summary_or_unit_name_or_unit_summary_or_unit_item_name_or_unit_item_summary_cont, :placeholder => "レッスン検索", :size => "13px", :class => "form-control" %>
              <%= f.submit '検索', :class => "btn btn-default" %>
            <% end %>
          </div>
          <ul class="nav navbar-nav navbar-right">
            <li><a href="/contact">お問合せ</a></li>
            <li>
              <% if user_signed_in? %>
                <%= link_to "ログアウト", destroy_user_session_path, :method => :delete %>
              <% else %>
                <%= link_to "ログイン", new_user_session_path %>
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
</div>
